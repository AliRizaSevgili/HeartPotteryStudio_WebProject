{{!-- layout: 'main' --}}
<div class="flex flex-col items-center justify-center min-h-screen bg-gray-100 text-center">
  <div class="bg-white shadow-lg rounded-lg p-6 max-w-lg">
    <h1 class="text-4xl font-bold text-red-500">{{errorCode}}</h1>
    <p class="text-lg text-gray-700 mt-2">{{errorMessage}}</p>
    <p class="text-gray-500">{{errorDetail}}</p>
    <a href="/" class="mt-4 inline-block bg-blue-500 hover:bg-blue-600 text-white px-6 py-3 rounded-lg text-lg transition-all">
      Go Back Home
    </a>
  </div>
</div>
